namespace OOP.Studies.Encapsulation
{
    internal class DbConnection
    {
        internal void OpenConnection(string connectionString)
        {
            // connect to database
        }

        internal void CloseConnection()
        {
            // close connection
        }
    }
}
